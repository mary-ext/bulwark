# Rust
/target
**/*.rs.bk
Cargo.lock.orig

# Node / web
node_modules/
.vite/
# NOTE: web/dist/ is intentionally committed so `cargo build` embeds a working
# UI without requiring Node. Regenerate it with `pnpm build` in web/.

# Editor / OS
.DS_Store
*.swp
.idea/
.vscode/

# Bulwark runtime data
/data/
*.log
config.local.yaml
